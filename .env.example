# Copy to .env.local and fill in. Ask the team lead for values — never commit .env.local.
GRAVV_API_KEY=grvSec_sandbox_xxx
GRAVV_CUSTOMER_ID=
GRAVV_ACCOUNT_ID=
GRAVV_WALLET_ADDRESS=
# Optional — enables Claude-written chase messages (template fallback without it)
ANTHROPIC_API_KEY=
